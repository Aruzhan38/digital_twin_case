# Digital Twin

A simple hackathon-ready starter project for a digital twin application with a FastAPI backend and React frontend.

## Run the backend

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload
```

## Run the frontend

```bash
cd frontend
npm install
npm run dev
```
